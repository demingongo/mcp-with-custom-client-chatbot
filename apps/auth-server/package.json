{
  "name": "auth-server",
  "version": "1.0.0",
  "description": "Authentication server with Kaapi and Saurbit",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsup src --format esm --outDir dist --clean --metafile --sourcemap",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@hapi/vision": "^7.0.3",
    "@kaapi/kaapi": "^0.0.44",
    "@kaapi/validator-zod": "^0.0.44",
    "@novice1/api-doc-generator": "^1.2.x",
    "@saurbit/oauth2": "^0.1.5",
    "@saurbit/oauth2-jwt": "^0.1.3",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "pug": "^3.0.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pug": "^2.0.10"
  }
}