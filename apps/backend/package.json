{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend with Model Context Protocol client",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsup src --format esm --outDir dist --clean --metafile --sourcemap",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "@hapi/boom": "^10.0.1",
    "@kaapi/kaapi": "^0.0.46",
    "@kaapi/validator-arktype": "^0.0.46",
    "@modelcontextprotocol/client": "^2.0.0-alpha.2",
    "arktype": "^2.2.0",
    "hapi-scalar": "^1.1.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  }
}