{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend with Model Context Protocol client",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsdown src/*.ts src/**/*.ts --format esm --out-dir dist --clean --sourcemap --unbundle --no-fixed-extension",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "@hapi/boom": "^10.0.1",
    "@kaapi/kaapi": "^0.0.51",
    "@kaapi/validator-arktype": "^0.0.51",
    "@modelcontextprotocol/client": "^2.0.0",
    "arktype": "^2.2.0",
    "hapi-scalar": "^1.1.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  }
}